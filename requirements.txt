# =============================================================================
# BMC-AutoInsight — Python 第三方依赖（从源码 import 抓取 + 开发与打包）
# 安装：pip install -r requirements.txt
# Python 建议：>= 3.10
# =============================================================================

# ---------- 运行时（必须） ----------
requests>=2.28.0
urllib3>=1.26.0
PyYAML>=6.0
Jinja2>=3.1.0
openpyxl>=3.1.0

# ---------- 开发 / 自动化测试 ----------
pytest>=7.0.0

# ---------- 可选：打包 Windows exe（build_exe.py / PyInstaller） ----------
pyinstaller>=6.0.0
